import React, { ReactElement } from 'react';
type DivProps = {
    height: number | string;
    width: number | string;
    border?: number;
    rounded?: number;
    loading?: boolean;
    children?: ReactElement;
    className?: string;
} & ({
    mode: "light";
} | {
    mode: "dark";
} | {
    mode: "custom";
    from: string;
    via: string;
    to: string;
});
declare function ShimmerDiv(props: DivProps): React.JSX.Element;
declare namespace ShimmerDiv {
    var defaultProps: {
        border: number;
        rounded: number;
        loading: boolean;
        className: string;
    };
}
export default ShimmerDiv;
