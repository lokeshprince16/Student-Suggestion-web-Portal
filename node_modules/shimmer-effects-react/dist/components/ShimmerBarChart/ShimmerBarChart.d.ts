import React, { ReactElement } from 'react';
type ChartProps = {
    barWidth?: number | string;
    chartType: "linear" | "random";
    loading?: boolean;
    children?: ReactElement;
    className?: string;
} & ({
    mode: "light";
} | {
    mode: "dark";
} | {
    mode: "custom";
    lineColor: string;
    from: string;
    via: string;
    to: string;
});
declare function ShimmerBarChart(props: ChartProps): React.JSX.Element;
declare namespace ShimmerBarChart {
    var defaultProps: {
        barWidth: string;
        loading: boolean;
    };
}
export default ShimmerBarChart;
