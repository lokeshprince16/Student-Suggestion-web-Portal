import React, { ReactElement } from "react";
type TableProps = {
    row: number;
    col: number;
    border?: number;
    borderColor?: string;
    rounded?: number;
    rowGap?: number;
    colPadding?: number[];
    loading?: boolean;
    children?: ReactElement;
    className?: string;
} & ({
    mode: "light";
} | {
    mode: "dark";
} | {
    mode: "custom";
    from: string;
    via: string;
    to: string;
});
declare function ShimmerTable(props: TableProps): React.JSX.Element;
declare namespace ShimmerTable {
    var defaultProps: {
        border: number;
        rounded: number;
        loading: boolean;
        rowGap: number;
        colPadding: number[];
        className: string;
    };
}
export default ShimmerTable;
